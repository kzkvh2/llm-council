# Copy to .env (gitignored) and fill in real values.
# This file documents which env vars the project needs — names only, no real values.

# OpenRouter API key — every model call in every stage goes through OpenRouter.
# Get one at https://openrouter.ai/ and make sure the account has credits.
OPENROUTER_API_KEY=
